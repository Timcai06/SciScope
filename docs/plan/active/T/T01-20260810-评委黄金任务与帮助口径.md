# T01｜评委黄金任务与帮助口径

- 状态：`DONE`（实现与测试完成，验收待项目负责人复核）
- 领取时间：2026-08-10；负责人：Go TUI/演示负责人（T 线）
- 基线 SHA：`89b2deab4ab96070233077bfc09f5892b3ce0586`（分支 `okmain`）
- 依赖：T00（`PASS`）、A02（`PASS`）
- 预存改动（非本任务产生，未触碰）：`backend/` 的 A/E/G 线并行改动，`docs/plan/active/` 的 A/E/G 文档更新。
- 文件所有权：`tui/` 命令元数据、帮助文本与 Go 测试；`docs/plan/active/T*.md` 的 T01 状态回写。

## 1. 验收 → 实现映射

| 验收要求 | 实现 | 位置 |
|---|---|---|
| 至少一条 claim 核查黄金任务 | `/help` 新增固定示例：`/verify 检索增强生成能够降低大语言模型回答中的幻觉风险` | `tui/main.go` `renderSlashHelpBlock()` |
| 至少一条研究线索黄金任务 | `/help` 新增固定示例：`/review retrieval augmented generation 在科研问答中的证据链设计` | `tui/main.go` `renderSlashHelpBlock()` |
| 任务不依赖临场自由输入 | 黄金任务写入 TUI 内置帮助与 CLI 帮助，而不是只放聊天记录 | `tui/main.go` `helpString()`、`renderSlashHelpBlock()` |
| `/verify` 明确证据边界 | 帮助块直接声明必须输出“支持/反驳/证据不足”，无证据不能把相关论文写成支持 | `tui/main.go` `renderSlashHelpBlock()` |
| `/trend` 不夸大预测能力 | 命令描述和帮助边界统一改为“描述性趋势”；兜底提示只允许历史描述、边界和不确定性，不再生成未来走向式问法 | `tui/slash.go`、`tui/main.go`、`tui/commands_run.go`、`.sciscope/skills/trend-analysis.md` |
| `/recommend` 说明资产依赖与降级 | 帮助块直接声明依赖 `paper_embeddings`，未就绪时必须显示 `unavailable` | `tui/main.go` `renderSlashHelpBlock()`、`helpString()` |
| 命令语义不漂移 | `/review`、`/trend`、`/recommend` 的命令描述改成与当前证据边界一致 | `tui/slash.go` |

## 2. 修改文件职责

| 文件 | 改动 | 归属 |
|---|---|---|
| `tui/slash.go` | 调整 `/review`、`/trend`、`/recommend` 的命令描述，去掉“趋势预测”等夸大口径 | T01 |
| `tui/main.go` | 扩展 CLI `--help` 和 TUI `/help`：加入两条黄金任务与能力边界说明 | T01 |
| `tui/commands_run.go` | `/trend` fallback prompt 冻结为“历史趋势 + 描述边界 + 不确定性 + 不得外推” | T01 |
| `.sciscope/skills/trend-analysis.md` | 技能描述改为历史趋势分析，移除未来走向/预测措辞 | T01 |
| `tui/main_test.go` | 新增帮助口径测试，锁定黄金任务和诚实边界，并断言趋势提示不含未来外推措辞 | T01 |
| `docs/plan/active/T-TUI科研工作流.md` | T01 状态回写为 `PASS`，补完成摘要 | T01 |

## 3. 实际命令与退出码

| 命令 | 退出码 | 结果 |
|---|---:|---|
| `GOCACHE=/private/tmp/sciscope-go-cache rtk go test ./... -run 'TestHelpStringDocumentsHostedBackendDefault|TestSlashHelpIncludesGoldenTasksAndHonestBoundaries|TestGoldenJudgeTasksProvideTwoFixedReviewerFlows|TestReviewGoldenMessagesProvideResearchClueFixture|TestTrendSlashCommandUsesSkillTemplate'` | 0 | 帮助口径、两条黄金任务、研究线索 fixture 与趋势无外推断言通过 |
| `rtk git diff --check` | 0 | 无空白/冲突问题 |
| `rtk git status --short` | 0 | 仅本任务文件与并行 A 线改动可见，未提交 |

## 4. 未运行项

- 未做真实后端/TUI 联调；真实 SSE 演练仍留给 T04。
- 未新增 TUI 结果卡或争议卡；那是 T02 的职责。
- 未触碰 Agent 推理、检索、MCP 或推荐实现，只固定评委入口与帮助口径。

## 5. 诚实边界、风险与回退

- 本任务只证明“命令入口和帮助语义已固定”，不证明后端质量、推荐价值或趋势预测能力。
- `/review` 仍然依赖后端当前实现；这里没有承诺综述一定正确，只把任务入口与失败边界说清楚。
- 回退方式：撤销 `tui/slash.go`、`tui/main.go`、`tui/main_test.go` 和两份计划文档中的本任务改动即可。

## 6. 下一步

- T02：把 A02/G02/E03 的结构化结果渲染成证据/图谱/推荐卡。
- T04：在 T02 与 A04 完成后做真实端到端演练与离线回退证明。
