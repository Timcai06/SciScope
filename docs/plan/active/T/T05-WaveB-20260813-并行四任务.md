# T05 Wave B｜并行四任务收口（T05-03 / 04 / 06 / 08）

- 状态：`DONE（待复核）`——产物已生成，验收待项目负责人复核
- 执行时间：2026-08-13
- 执行分支：`feat/tui-grok-alignment`（基于 T05-02 提交 `dc665bc`）
- 执行方式：主线（T05-04）+ 3 个并行子代理（T05-03/06/08，按计划第十节 Wave B 文件租约，子代理只写各自新文件，接线由主线统一完成）

## 一、T05-03｜Welcome 与 ASCII 启动页（子代理 + 主线接线）

- **产物**：`tui/view_welcome.go`（`renderWelcome(width, sessions, hosts)` 纯函数）+ 7 个测试。
- **变化**：三栏 Quick Actions/System Status/Golden Demo 与外层大 Card 删除；层级 = ASCII Logo（≥86 完整 / 60–85 紧凑 / <60 纯文本）→ tagline → `/verify /review /trend /demo` 提示 → Recent（最多 3 条）→ faint 脚注（`backend local · /doctor 检查后端与 LLM`，URL 派生不伪造）。
- **验证**：`TestWelcomeRendersAtThreeWidths` 等 7 测试全绿；真实 PTY 120 列首屏截图达标（快照 `welcome-120col-waveb.raw/.txt`）；5 个旧 splash 测试更新为新验收。

## 二、T05-04｜Conversation 与 Block Grammar（主线）

- **产物**：typed running block 接入真实流 + 无框 grammar 基础。
- **变化**：
  1. streaming：textMsg 首 chunk 创建 `BlockAnswer` running 块（同一 ID 更新），doneMsg 转正（`setBlockContent` 正式 Glamour 渲染）；
  2. running 块 animated accent：`⏺ 研究结论` + 正文 + `▌` 光标；
  3. 割裂的 live answer 预览 shelf（`renderLiveAnswerPreview`）删除——答案实时进 scrollback；
  4. 连续 research tools 语义 grouping：组首 `⏺`、组内缩进 `⎿`，tool_result 延续组、非 tool 断组；
  5. 发送后锚定 feature gate：`SCISCOPE_TUI_SEND_ANCHOR=1` 时本轮第一个响应事件把用户问题锚到 viewport 顶（未验证不锁死）。
- **验证**：`TestConsecutiveToolCallsAreGrouped`、running 光标渲染测试、`TestStreamingTextUpdatesRunningBlockInScrollback`（同一 ID 更新断言）全绿；demo 80 列快照 `demo-80col-t05-04.raw/.txt`。

## 三、T05-06｜Evidence、Claim 与 Answer Contract 层级（子代理 + 主线接线）

- **产物**：`tui/view_evidence.go`（`RenderEvidencePapersCard` / `RenderClaimEvidenceCard` / `RenderEvidenceInsufficientCard` / `RenderAnswerContractCompressed` / `RenderAnswerContractFull`）+ 10 个测试。
- **变化**：
  1. Evidence 卡用 `domainCard` 基元（强领域对象，允许边框）：Title/Year/Author/PaperID、Stance、Confidence 与 Similarity 分开、EvidenceSentence 仅 display policy 允许、ChunkUID/provenance faint 审计、hidden reason；
  2. Insufficient 用 Warning（不是 Error）、明确「系统拒绝无证据结论」、不显示假 confidence；
  3. Answer Contract 默认压缩一行（`部分支持 · N 条证据 · 引文已验证`），完整 schema 保留在 `RenderAnswerContractFull`；
  4. contradictory 与 supporting 视觉权重相当；
  5. 证据类工具结果 `BlockEvidence` kind 接入。
- **验证**：10 个测试全绿（含 display-policy-blocked 不泄漏、confidence/similarity 同屏不混淆）；修子代理测试笔误 1 处（0.81 → 中置信）；旧 sidecar 测试更新为压缩行语义。

## 四、T05-08｜Overlay / Picker / Command Palette（子代理 + 主线接线）

- **产物**：`tui/view_overlay.go`（`OverlayState`/`PickerItem`/`PickerSection`/`OverlayKind`/`OverlayFooter` + `buildOverlaySections`/`filterOverlaySections`/`renderPickerOverlay`/`escBehavior`）+ 11 个测试。
- **变化**：Slash Commands、Theme、Sessions/Resume、Tools、Doctor、Clear/Quit confirm 六类统一 grammar：搜索框 + 分组 + `❯` 选中态（AccentSoft 背景）+ shortcut 右对齐 + desc 第二行 faint + footer（`↑↓ 导航 · Enter 选择 · Esc 关闭`）+ 可视窗口跟随选中滚动。
- **验证**：11 个测试全绿；修子代理 2 处实现 bug（lipgloss `Width(W)` 实测总宽 = W+2 → 传 `width-2`；`Width(contentW)` 导致 separator 软换行）；真实 PTY palette 画面达标（快照 `palette-80col-waveb.raw/.txt`）；2 个旧 palette 测试更新为新 grammar。

## 五、Wave B 整体验证

| 项 | 结果 |
|---|---|
| 全量 `go test ./...` | PASS 5.5s（新增 28 个分支测试 + T05-04 测试，既有测试零回归） |
| `gofmt` / `go vet` | 干净 |
| demo 80 列边框字符 | T05-02：829 → Wave B：**652（-21%）** |
| Welcome 首屏 | Logo 第一视觉、无 Dashboard、脚注不伪造（120 列快照） |
| Palette 交互 | 统一 Overlay 画面（80 列快照） |

快照目录：`output/evidence/t05/baseline/`（`demo-80col-waveb.*`、`palette-80col-waveb.*`、`welcome-120col-waveb.*`）。

## 六、遗留与后续

- T05-05（Research Trace 折叠）、T05-07（Composer/Status）、T05-09（Scroll/Focus/Mouse/Resize）未开始；
- `escBehavior` 类型已交付但 Esc 键位处理仍走现有逻辑（行为等价，待 T05-09 统一接线）；
- 发送后锚定 `SCISCOPE_TUI_SEND_ANCHOR=1` 需人工终端验证（T05-09 交互矩阵）；
- `renderToolResult` 中 trend/recommend 分支暂走旧路径（非本波任务）。

## 七、状态与复核请求

**四个任务均标 `DONE（待复核）`**。请项目负责人复核：

1. `tui/view_welcome.go`、`tui/view_evidence.go`、`tui/view_overlay.go` 及各自测试；
2. 三个视觉快照（Welcome/Palette/demo 边框对比）；
3. 实际运行 `make tui-demo` 与 `/` 启动器体验；
4. 复核通过后解锁 T05-05 / T05-07（可并行，文件已隔离）与 T05-09（串行集成）。
