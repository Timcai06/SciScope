# T03｜不可用依赖的诚实降级

- 状态：`DONE`（实现与测试完成，验收待项目负责人复核）
- 领取时间：2026-08-05；负责人：Go TUI/演示负责人（T 线）
- 基线 SHA：`67e275a6d90dafb2b64ae261bb5932acd708a7bc`（分支 `main`）
- 依赖：T00（`PASS`，待复核）
- 预存改动（非本任务产生，未触碰）：工作区 `backend/`（A 线）、`docs/plan/active/` 下
  A/D/E/G 线文档改动。
- 文件所有权：`tui/` 的错误状态、重试和帮助文本及测试；**未绕过服务端安全控制**
  （权限拒绝不自动放行，只提示只读替代与联系管理员）。

## 1. 验收 → 实现映射

| 验收要求 | 实现 | 位置 |
|---|---|---|
| 区分 API 不通 | 原有分类保留：`connection refused`/`无法连接后端` → 本地提示 `make backend`、托管提示 `/demo` | `render.go` `recoveryActionForBackend` |
| 区分数据库/向量不可用 | 数据库（`database`/`postgres`/`pgvector`）原有；**新增**向量/embedding 资产缺失（`paper_embeddings`/`does not exist`/`undefinedtable`/`embedding`）→ "数据/向量资产不可用"，提示 `make embeddings` 且"资产缺失期间如实降级，不伪造结果" | `render.go` `recoveryAction` |
| 区分 LLM 超时 | **新增**：`llm`+（`timeout`/`timed out`）→ "LLM 超时"；通用 `timed out`/`timeout`/`gateway` → "请求超时"，均提示 `/retry` 与 `/doctor`，可重试但由用户手动触发（不无限重试） | `render.go` `recoveryAction` |
| 区分无证据 | `verify_claim` 返回"证据不足"时，结果卡与 Markdown 摘要显式展示"证据不足: <理由>"（`claimResult` 新增 `理由` 字段），空证据不再渲染成无解释卡片 | `types.go`、`render.go` |
| 区分权限限制 | **新增**：`forbidden`/`permission denied`/`403` → "权限限制"，`Retryable=false`（不进入重试循环），提示改用只读方式或联系管理员 | `render.go` `recoveryAction` |
| 会话可保存/重试且不丢失失败原因 | **修复**：`doneMsg` 原先仅在有最终答案时保存会话，纯失败轮次（仅 `error` 事件）会丢失失败原因；现失败轮次同样落盘并提示"会话已保存(含失败原因)"，`/export`、`/resume` 可回看；`/retry` 保持可用（`lastQuestion` 不受影响） | `main.go`（`model.lastTurnErr`、`errMsg`、`doneMsg`） |
| 帮助文本说明降级行为 | `/help` 帮助块新增提示："服务或数据不可用时, 按错误面板提示操作: /doctor 查看状态, /retry 重试, 失败原因会随会话保存。" | `main.go` `renderSlashHelpBlock` |
| 不伪造结果、不无限重试 | 错误面板只给下一步命令；中断/超时（ctx 取消）不发伪造错误帧（有测试断言）；权限错误不可重试 | `render.go`、`stream.go` |

## 2. 修改文件职责

| 文件 | 改动 | 归属 |
|---|---|---|
| `tui/render.go` | `recoveryAction` 新增 4 分类（权限、LLM 超时、请求超时、向量资产）；verify 卡与摘要展示"证据不足"理由 | T03 |
| `tui/types.go` | `claimResult` 新增 `Reason`（`json:"理由"`）字段 | T03 |
| `tui/main.go` | `model.lastTurnErr` 字段；`errMsg` 记录失败；`doneMsg` 失败轮次保存会话；`/help` 降级提示 | T03 |
| `tui/main_test.go` | 新增 8 个测试（见 §3） | T03 |

## 3. 验证

- `rtk go test ./...`（`tui/`）：**100 passed in 1 packages**，退出码 0（改动前 91，新增 8，含 1 处修正后全绿）。
- 新增测试（模拟 SSE 错误/超时 + 分类 + 会话）：
  - `TestStreamEmitsBackendErrorFrame`：httptest 返回 SSE `error` 帧 + `[DONE]`，断言错误透传
  - `TestStreamSurfacesConnectionFailure`：后端不可达，断言"无法连接后端"
  - `TestStreamCancelOnTimeoutDoesNotFabricateError`：ctx 超时（等同用户中断），断言**不**伪造错误
  - `TestRecoveryActionClassifiesTimeout` / `...LLMTimeout` / `...PermissionDenied` / `...VectorAssetMissing`
  - `TestFailedTurnStillPersistsSession`：仅 error 无 final 的轮次落盘，断言含"错误与恢复建议"+ 失败原因
  - `TestVerifyClaimNoEvidenceShowsReason`：证据不足卡展示理由（含 Markdown 摘要）
- `gofmt -l tui/` 无输出；`rtk git diff --check` 退出码 0。

## 4. 未运行项

- 本机后端/LLM 未启动，未做真实错误触发演练（真实端到端留 T04；本任务用 httptest 模拟 SSE 帧与连接失败）。
- 未改动服务端权限规则、工具注册表或数据层——权限/校验语义以后端为准。

## 5. 风险/回退

- 分类基于错误文本子串匹配（与既有 `recoveryAction` 同风格）；新文本模式落入 default"请求失败"仍有兜底提示，不丢可读性。
- 回退：撤销本任务对 `tui/` 4 个文件的改动即恢复原状；不涉及数据或服务端变更。
- 下游：T04 演练时可把本机真实降级路径（数据库停止、LLM 停止）纳入验收；T02 展示层可复用"数据/向量资产不可用"状态卡。
