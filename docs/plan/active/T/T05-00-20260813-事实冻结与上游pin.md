# T05-00｜事实冻结与 Grok 上游参考基线

- 状态：`DONE（待复核）`——产物已生成，验收待项目负责人复核
- 执行时间：2026-08-13
- 执行 agent：Reasonix（提交 `17a7c0d` 之后的工作在分支 `feat/tui-grok-alignment`）
- 依赖：T04 `PASS`
- 产物位置：`output/evidence/t05/`

## 一、冻结事实（SciScope 侧）

| 项 | 值 |
|---|---|
| 代码基线 HEAD | `bc2cc4316bfd9fc41f4030059543ce6c6c505ede`（与计划头部远端观察基线一致，T05-00 未改任何运行代码） |
| 计划文档提交 | `17a7c0d`（`docs(tui): add T05 grok-build alignment plan and vendor ignore`） |
| 工作分支 | `feat/tui-grok-alignment`（按计划第十五节推荐） |
| Go | `go1.26.4 darwin/arm64` |
| OS | macOS 26.5.2 (Build 25F84) |
| 终端环境 | `TERM=xterm-256color`、`COLORTERM=truecolor`；执行 shell 非交互（PTY 工具补窗口尺寸） |
| 工作区状态 | 冻结时 `git status --short` 干净（仅 T05 计划文档与 .gitignore 两个新文件已提交） |

## 二、上游 pin（Grok Build 侧）

| 项 | 值 |
|---|---|
| 仓库 | `xai-org/grok-build`（公开，Apache-2.0） |
| GitHub commit | `e5fd4816d43260c15ba785f103990c1ed6cea230`（main，28 commits） |
| `SOURCE_REV` | `ea094a8c369475f97c85540d01730baec0dce5d6` |
| 克隆 | `git clone --depth 1 https://github.com/xai-org/grok-build.git .vendor/grok-build`（`.vendor/` 已加入 `.gitignore`） |
| 版本策略 | 按计划：禁止跟随 floating main；本 commit 即 T05 全阶段固定参考版本 |

## 三、基线验证（T05-00 门禁逐项）

1. `git status --short` 干净 ✅（见冻结事实）
2. `git rev-parse HEAD` 记录 ✅（`bc2cc43`）
3. `cd tui && go test ./...` ✅ **PASS 5.260s**（real 6.267s），无既有失败需登记
4. `make tui-demo` 等价执行 ✅：`go build` + PTY 播放（`SCISCOPE_TUI_DEMO_DELAY_MS=15`），80/120/160 三宽度均正常渲染
5. 80/120/160 列基线 ✅（见下）
6. 七类画面记录 ✅（见下；Evidence Insufficient 登记缺口）
7. Grok commit 与 `SOURCE_REV` ✅
8. 源码对标矩阵 ✅（`output/evidence/t05/upstream-map/source-map.md`）

## 四、基线产物清单

```text
output/evidence/t05/
├── baseline/
│   ├── demo-80col.raw/.txt       # 25,877 B / 1,933 行
│   ├── demo-120col.raw/.txt      # 32,537 B / 1,933 行
│   ├── demo-160col.raw/.txt      # 41,319 B / 1,933 行
│   ├── palette-80col.raw/.txt    # Command Palette 交互画面（`/` 打开）
│   └── sessions-80col.raw/.txt   # Session Picker 交互画面（`/sessions` 执行）
└── upstream-map/
    └── source-map.md             # 21 行对标矩阵 + do-not-copy 清单 + 计划差异记录
```

- raw = PTY 原始流（含 ANSI），txt = ANSI 归一化文本（脚本 `tmp/ptydump.py`、`tmp/keydump.py`、`tmp/normalize.py`）。
- 截图基线：T05-00 以文本渲染基线为准（可复核、可 diff）；真实终端截图属人工视觉证据，留 T05-11 terminal matrix。

## 五、七类画面覆盖核对

| 画面 | 状态 | 证据 |
|---|---|---|
| Splash（ASCII Logo + Dashboard） | ✅ | demo 首帧（三宽度） |
| 普通回答（结论） | ✅ | demo 帧：`结论 该论断获得强支持…` |
| 工具调用 | ✅ | demo 帧：`⏺ 检索文献`、研究计划 4 步骤、`⏺ 论断核查` |
| Evidence | ✅ | demo 帧：`╭─ evidence · 证据卡 3 篇`（标题/年份/作者/摘要句）+ `╭─ verify · 论断核查 · 强支持 · 0.846`（SUPPORT/相似度/置信/审计链） |
| Evidence Insufficient | ⚠️ 缺口 | demo 脚本不覆盖；需后端 fixture 或人工记录（登记给 T05-06） |
| Command Palette | ✅ | `palette-80col.txt`：常用/会话/证据/系统四分组 + 选中态 |
| Session Picker | ✅ | `sessions-80col.txt`：8 个历史会话 + `/resume N` 语法 |
| Doctor | ✅ | `sciscope-tui doctor` 文本报告（Backend/LLM warn 不可达为当前环境预期，非 T05 引入） |
| Streaming | ✅ | demo 过程帧（研究计划 running 状态 → 完成） |

## 六、代码规模统计（T05-10/11 对比基准）

| 文件 | 行数 |
|---|---|
| `tui/main.go` | 1,898 |
| `tui/render.go` | 1,205 |
| `tui/main_test.go` | 2,054 |
| `tui/commands_run.go` | 331 |
| `tui/doctor_demo.go` | 245 |
| `tui/contracts_test.go` | 226 |
| `tui/contracts.go` | 176 |
| `tui/stream.go` | 123 |
| `tui/types.go` | 116 |
| `tui/slash.go` | 106 |
| **tui 合计** | **6,480** |
| go test 耗时 | 5.26 s |

## 七、许可证边界（TX01 输入）

- 根 `LICENSE`：Apache-2.0，© 2023-2026 SpaceXAI；first-party 代码整体该许可。
- `THIRD-PARTY-NOTICES`（约 760 KB）：依赖许可证汇总，以 MIT/Apache-2.0/BSD 等 permissive 为主。
- `third_party/`（vendored）：`mermaid-to-svg`（MIT）、`dagre_rust` 等（Apache-2.0），各带自身 LICENCE/NOTICE。
- `crates/codegen/xai-grok-tools/THIRD_PARTY_NOTICES.md`：openai/codex（Apache-2.0）与 sst/opencode（MIT）移植附 §4(b) 变更声明——T05 不改 tools，无需 port 该区域。
- pager UI 依赖（ratatui/crossterm/unicode-width 等）均 MIT/permissive。
- **SciScope 策略**：行为级重实现优先；不复制品牌资产与产品文案；任何直接 port 必须按 TX01 记录完整 provenance；grok-build 是正式开源仓库（Apache-2.0），与 `~/DEV/claude-code源码`（泄漏源码，不可参考）性质完全不同，T05 对标仅以 grok-build 为准。

## 八、计划文档差异记录（已写入 source-map.md 第六节）

计划第六节路径清单经实际核对全部有效；细化三处：`src/app/actions.rs` 与独立 `src/actions/` 目录并存；`src/scrollback/` 为 16 文件模块；`welcome/`、`prompt_widget/` 为目录。矩阵已按实际子文件记录出处。

## 九、状态与复核请求

**T05-00 标 `DONE（待复核）`**。请项目负责人复核：

1. 基线证据：`output/evidence/t05/baseline/`（10 个文件）与 `output/evidence/t05/upstream-map/source-map.md`；
2. 上游 pin：`.vendor/grok-build` 当前 HEAD 应为 `e5fd4816`；
3. Evidence Insufficient 画面缺口是否接受留到 T05-06 或需 T05-00 补充；
4. 复核通过后解锁 T05-01（Theme 与呈现 primitive）。
