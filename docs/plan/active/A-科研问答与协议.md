# A｜科研问答与协议

- 状态：`PENDING`（A00、A01 `PASS`；A02 等待 D05、E02）
- 负责人：Agent/后端负责人；复核：检索负责人 + 项目负责人
- 当前领取：等待 D05、E02 → A02；依赖上游：[D 文献内容结构化](D-文献内容结构化.md)、
  [E 证据 L3 与国赛证明](E-证据L3与国赛证明.md)
- 交付边界：以现有 Agent 工具注册表、REST/SSE 合同回答有证据的问题；不新建第二套 Agent
  编排，不把模型流畅表达当成文献支持。

## 已确认事实与诚实边界

| 事实 | 代码依据 | 不能直接推出 |
|---|---|---|
| 后端具备 Agent 工具、对话路由和 SSE 事件链 | `backend/app/agent/`、`backend/app/api/routes_agent.py` | 每个自然语言答案都已被人工评测为正确 |
| 领域综述、论文详情、趋势、推荐、图谱和核查分别已有工具入口 | `backend/app/agent/tools/` | 工具组合一定满足用户问题或完整运行 |
| `verify_claim` 是 L3 证据路径 | `backend/app/agent/tools/verify_claim.py` | 其当前输出已达到金标准质量 |

## 最简依赖图

```text
A00 现有工具盘点 → A01 意图路由 → A02 答案/引用合同 → A03 失败与回归评测 → A04 API/SSE 证明
D05 结构化出口 ───────────────────────────────┘
E02 L3 纪律 ─────────────────────────────────┘
```

## 原子任务

### A00｜能力盘点与问题边界

- 状态：`PASS`（2026-08-05；证据见 [A/A00-20260805-能力盘点与问题边界](A/A00-20260805-能力盘点与问题边界.md)）；依赖：无。
- 只读范围：`backend/app/agent/`、路由、既有评测与接口文档。
- 做什么：把“文献问答、领域综述、论断核查、图谱查询、推荐、趋势”映射到真实 tool、输入、
  输出、数据依赖和当前验证等级；删除任何没有实际路径的产品表述。
- 验收：形成一张能力注册表；每项明确是否可在当前运行环境执行。
- 验证：静态路径核对 + 一条在线/阻塞运行记录。
- 完成摘要：11 个原生工具逐一映射；六类能力中推荐在当前环境阻塞（`paper_embeddings` 表
  缺失，对应 G03）；语义检索臂覆盖率 2.1%；6 处文档口径与代码不一致已列清单待修订；
  在线运行记录 6/6（含 2 条明确阻塞/降级记录）。

### A01｜意图路由与工具选择

- 状态：`PASS`（2026-08-05 独立复核通过；证据见
  [A/A01-20260805-意图路由与工具选择](A/A01-20260805-意图路由与工具选择.md)）；依赖：A00。
- 文件所有权：`backend/app/agent/` 的路由/工具选择及测试；不在 TUI 重写推理。
- 做什么：为不同问题选择正确工具，尤其区分“找相关论文”“总结领域”“判定某 claim 是否有
  支持/反驳证据”；信息不足时追问或拒答，不能偷换为普通检索。
- 验收：至少覆盖上述三类问题和未知意图；工具选择可从 SSE/日志看见且不泄露内部提示。
- 验证：固定测试集、工具调用断言、无工具/超时降级测试。
- 完成摘要：新增确定性意图层 `intents.py`（16 意图/20 组固定测试集）；`intent` SSE 事件
  （label+reason，不泄露引导提示）；claim 问题首轮只要没有 `verify_claim` 即确定性替换
  （含模型错误调 `search_literature` 的回归）；vague/unknown 注入澄清约束；全量测试 336
  passed；4 场景在线运行记录。

### A02｜答案、引文与不确定性合同

- 状态：`PENDING`；依赖：A01、D05、E02。
- 文件所有权：Agent 输出 schema、SSE 事件、API schema、测试；展示由 T 线实现。
- 做什么：每个可验证结论返回来源 paper/chunk、引用范围、能力类型和不确定性；将“未找到”
  与“证据不足”分开，L3 结论带 evidence sentence/校准拒答信息。
- 验收：结构化输出可被 API、MCP 和 TUI 同时消费；无引文的关键结论被拒绝或标为生成性说明。
- 验证：schema 测试、序列化测试、正反/不足三类黄金样本。

### A03｜科研问答回归与对抗集

- 状态：`PENDING`；依赖：A02。
- 文件所有权：`evaluation/`、Agent 回归样本及报告；不修改真实论文。
- 做什么：扩展当前少量会话回归，加入错误引用、确认偏误、时间越界、相关/因果混淆、无证据
  追问和工具不可用案例；人工复核答案支持度与引用精确性。
- 验收：每类有明确分母；失败不被静默改写为成功；在线模型评测与 CI 静态测试分开报告。
- 验证：任务实现后的 `rtk make eval-dialogue` 和人工评分记录。

### A04｜API/SSE 端到端证明

- 状态：`PENDING`；依赖：A02、A03。
- 文件所有权：API 路由、SSE 合同测试、开发者文档；MCP 特定证明留给 E04。
- 做什么：证明一个自然语言科研问题从 API 接入、工具调用、事件流到带证据结果的完整路径；
  将数据库/embedding/LLM 不可用显示为明确降级事件。
- 验收：有成功、无证据、依赖故障三类可复现 trace；API 响应不伪装未完成能力。
- 验证：端到端测试、SSE fixture、一次实际 trace（或环境阻塞记录）。

## 证据台账

| 任务 | 状态 | PASS 所需证据 |
|---|---|---|
| A00 | `PASS` | tool—能力—运行态注册表（[A/A00-20260805-能力盘点与问题边界](A/A00-20260805-能力盘点与问题边界.md)） |
| A01 | `PASS` | 路由测试与错误意图样本（[A/A01-20260805-意图路由与工具选择](A/A01-20260805-意图路由与工具选择.md)） |
| A02 | `PENDING` | 输出 schema 与引文/拒答测试 |
| A03 | `PENDING` | 回归主表、人工复核与失败例 |
| A04 | `PENDING` | API/SSE 三情形端到端证据 |
