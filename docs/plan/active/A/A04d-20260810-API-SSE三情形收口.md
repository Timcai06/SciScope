# A04d｜API/SSE 三情形收口

- 状态：`PASS（工程 live 门禁；非远端 canonical / 非人工质量证明）`
- 日期：2026-08-10
- 依赖：A04a、A04b、A04c。
- 范围：只汇总真实运行证据和诚实边界，不新增第二套 Agent 或评测口径。

## 一、三情形证据

| 情形 | 真实结果 | 证据 | 工程结论 |
|---|---|---|---|
| 成功 | DeepSeek + 旧全量级公开 Mac 快照；`completed`、`partially_supported`、4 条引用、`citation_compliance=ok` | `output/evidence/a04/success/deepseek_agent.response.json`、`deepseek_agent_stream.raw.sse`、`deepseek_agent_stream.summary.json` | `PASS` |
| 证据不足 | DeepSeek + 同一快照；`completed`、`evidence_insufficient`、校准拒答、API/SSE 最终态一致 | `output/evidence/a04/insufficient/deepseek_fullsize_agent.response.json`、`deepseek_fullsize_agent_stream.raw.sse`、`deepseek_fullsize_summary.json` | `PASS` |
| 模型不可用 | 隔离的不可用模型端点；API 与 SSE 均显式 `stop_reason=no_model`，不产生结构化成功 | `output/evidence/a04/model_unavailable/agent_no_model.json`、`agent_stream_no_model.sse`、`cleanup_probe.txt` | `PASS`（显式降级） |

对应原子报告：

- [A04a 成功 API/SSE 链路](A04a-20260810-成功API-SSE链路.md)
- [A04b 证据不足 API/SSE 链路](A04b-20260810-证据不足API-SSE链路.md)
- [A04c 模型不可用 API/SSE 链路](A04c-20260810-模型不可用API-SSE链路.md)

## 二、验证

- 成功和证据不足两条链路均来自真实 `/api/agent` 与 `/api/agent/stream`，生成模型为 `deepseek-chat`。
- SSE 均包含 intent、plan、工具事件、final 与字面量 `[DONE]`。
- 模型不可用专项与预算回归：`12 passed`；Wave 4 全量后端回归：`539 passed, 1 skipped, 5 warnings`。
- 所有证据只使用既有公开文献快照；未接入或发送讯飞数据。
- 本轮三条 canonical live 证据不保留 API key、数据库 DSN、密码、IP 或具体临时端口。历史 mock 回包保留为早期回归记录，不作为本轮 canonical 结论依据。

## 三、严格边界

本任务只证明：

- 现有 Agent 能通过真实 HTTP/SSE 运行成功、证据不足和模型不可用三种工程状态；
- `answer-contract/v1` 能在成功与证据不足路径进入最终响应；
- 模型不可用不会 silent success。

本任务不证明：

- 远端 canonical `159,164 papers / 367,861 chunks` 已完成同一套 A04 trace；
- tool-level 数据库/检索故障一定输出 `structured_answer.status=dependency_failure`；
- 人工答案支持度、引用精确率或真实科研效率已经提高；
- 讯飞数据已经准入或参与运行。

因此 A04 工程门禁可以解锁 T04 演练，但远端 canonical 演练、离线回退和评委材料仍由 T04/E07 验收。
