# A04a｜真实 API/SSE 成功链路

## 1. 任务结论

- **实现状态**：真实 `/api/agent` 与 `/api/agent/stream` 已完成一次 live 成功运行。
- **自动验证状态**：新增 live 原始响应与 SSE 已核对，聚合响应 `stop_reason=completed`，SSE 含 `tool_call/tool_result/final`，`structured_answer` 合同完整。
- **live 工程证明状态**：**`PASS`**，但范围严格限定为：
  - **Mac 当前 public-only SciScope 旧全量级快照库**
  - **DeepSeek live 工程链路可运行**
  - **不是远端 canonical 159164/367861 重建状态**
  - **不是质量证明，也不是国赛外部应用证明**

本任务现在已经有真实成功链路；此前的 bind / DNS / no-model 阻塞记录仅保留为历史尝试，不再代表最终状态。

## 2. 独占产物

| 路径 | 用途 |
|---|---|
| `output/evidence/a04/success/request.redacted.json` | 脱敏请求体 |
| `output/evidence/a04/success/real_readyz.json` | 真实 provider 配置下的 `/readyz` 成功结果 |
| `output/evidence/a04/success/deepseek_agent.response.json` | live `POST /api/agent` 聚合响应 |
| `output/evidence/a04/success/deepseek_agent_stream.raw.sse` | live `POST /api/agent/stream` 原始 SSE |
| `output/evidence/a04/success/deepseek_agent_stream.frames.jsonl` | live SSE 逐帧解析结果 |
| `output/evidence/a04/success/deepseek_agent_stream.summary.json` | live SSE 摘要：事件序列、tool、structured answer、snapshot scope |
| `output/evidence/a04/success/mock_readyz.json` | mock provider 配置下的 `/readyz` 结果 |
| `output/evidence/a04/success/mock_agent.headers.txt` | mock `POST /api/agent` 的真实 HTTP 头 |
| `output/evidence/a04/success/mock_agent.response.json` | mock `POST /api/agent` 聚合响应 |
| `output/evidence/a04/success/mock_agent_stream.headers.txt` | mock `POST /api/agent/stream` 的真实 HTTP 头 |
| `output/evidence/a04/success/mock_agent_stream.raw.sse` | mock SSE 原始事件流 |
| `output/evidence/a04/success/mock_agent_stream.frames.jsonl` | 解析后的 SSE 帧 |
| `output/evidence/a04/success/mock_agent_stream.summary.json` | 事件序列、`stop_reason`、`structured_answer` 缺失情况 |
| `output/evidence/a04/success/deepseek_route_blocked.txt` | 用户授权后仍被执行环境阻断的 live 路由证据 |
| `output/evidence/a04/success/blockers.md` | 阻塞事实与下一步条件 |
| `output/evidence/a04/success/cleanup.txt` | 8111 端口清理证明 |

## 3. 实际命令与退出码

| 命令 | 退出码 | 结果 |
|---|---:|---|
| `psql -d sciscope -c 'select count(*) from papers;'` | 0 | `159187`，确认 host DB 可读 |
| `psql -d sciscope -c 'select count(*) from paper_chunks;'` | 0 | `367773` |
| `psql -d sciscope -c 'select count(*) from chunk_embeddings;'` | 0 | `367773` |
| `psql -d sciscope -c 'select count(*) from paper_embeddings;'` | 0 | `159135` |
| `curl -fsS <temporary-backend>/readyz`（real provider） | 0 | `db/retrieval/model` 全部 `configured` |
| live `POST /api/agent`（DeepSeek） | 0 | `stop_reason=completed`，`model=deepseek-chat`，`steps=4` |
| live `POST /api/agent/stream`（DeepSeek） | 0 | `519` 个事件，尾部 `final -> done`，`structured_answer.status=partially_supported`，`citation_compliance=ok` |
| `curl -fsS <local-llm>/v1/models` | 7 | 本地模型端口未监听 |
| mock `POST /api/agent` | 0 | HTTP `200`，但返回 `stop_reason=no_model` |
| mock `POST /api/agent/stream` | 0 | HTTP `200`，SSE 序列仅 `final -> done`，无 tool call、无 citations、无 `structured_answer` |
| sandbox 内启动 `8121` live backend | 1 | 绑定端口失败：`operation not permitted` |
| `TestClient` 直打真实 `/api/agent` | 1 | 跑到 `llm_step` 后 DeepSeek 外联失败：`urllib.error.URLError [Errno 8]` |
| 最终 `curl -fsS <temporary-backend>/readyz`（cleanup 后） | 7 | 确认临时后端已关闭 |
| `rtk git diff --check` | 0 | 通过 |

## 4. 关键事实

### 4.1 真库已就绪

`real_readyz.json` 证明这次不是 sample mode：

- `db.status = configured`
- `retrieval.status = configured`
- `model.status = configured`

其中 `model=configured` 只证明**存在云端 DeepSeek 配置**，不等于本地模型可用，也不等于已经获得外发授权。

### 4.2 live 成功链路已被真实抓到

`deepseek_agent.response.json` 与 `deepseek_agent_stream.summary.json` 共同证明：

- 聚合响应：
  - `stop_reason = "completed"`
  - `model = "deepseek-chat"`
  - `steps = 4`
  - `tools_used = ["search_literature", "verify_claim", "get_paper", "get_paper"]`
- `structured_answer`：
  - `status = "partially_supported"`
  - `answer_mode = "evidence_based"`
  - `citation_compliance = "ok"`
  - `tool_basis = ["verify_claim"]`
  - `citations_count = 4`
- SSE：
  - 首部事件序列含 `intent -> plan -> tool_call -> tool_call -> tool_progress...`
  - 尾部为 `... -> final -> done`
  - 总事件数 `519`

这已经满足 A04a 对“真实 API/SSE 成功链路”的工程证明要求。

### 4.3 mock 传输链仍然保留，但只作为对照

`mock_agent_stream.summary.json`：

- `http_status = 200`
- `event_sequence = ["final", "done"]`
- `stop_reason = "no_model"`
- `structured_answer_present = false`
- `tool_basis = null`
- `citations = null`

它现在只说明：在**无本地模型**条件下，mock/本地模型路径会在工具执行前终止；它不再代表本任务最终状态。

### 4.4 历史阻塞已转为背景信息

以下内容保留在 `blockers.md` / `deepseek_route_blocked.txt`，仅作为 2026-08-10 的历史尝试背景：

- 本地模型缺失时的 `stop_reason=no_model`
- 首轮外发审批拒绝
- 某些执行面上的 bind / DNS 失败

它们说明调试路径存在过阻塞，但**不否定**当前已经拿到的 live success evidence。

## 5. 下一步解锁条件

A04a 的工程目标已完成；下一步不是“继续解锁 A04a”，而是正确使用这份证据：

1. 在 A 主线中把它当作 **legacy full-size snapshot 的 live engineering proof**
2. 不要把它写成：
   - 远端 canonical 2080 Ti / 讯飞数据重建证明
   - 检索/stance/问答质量主表
   - 外部用户价值证明
3. 若后续需要国赛级 stronger proof，应在远端 canonical 语料或正式评测集上另建 A04b/A04c/E 线证据

## 6. 文件边界与未触碰项

- 未修改 A 主计划、`active/README`、A02/A03 合同、TUI、报告、G/E/D 线。
- 未启动远端 2080 Ti、未跑 embedding、未提交或推送。
- 本报告中的 live 语料上下文均指 **Mac 当前 public-only SciScope 旧全量级快照库**，不是远端 canonical `159164/367861` 讯飞/2080 Ti 重建状态。
- 本任务交付的是**工程可运行性证据**，不是质量结论、不是用户研究、不是提交级外部证明。
