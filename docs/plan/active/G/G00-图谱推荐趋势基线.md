# G00｜图谱、推荐与趋势基线（报告）

- 状态：`PASS`（只读盘点完成；推荐线受 G03/算力阻塞）
- 领取时间：2026-08-05；负责人：检索/图谱负责人（G 线）
- 基线 SHA：`67e275a6d90dafb2b64ae261bb5932acd708a7bc`（分支 `main`）
- 预存改动（非本任务产生，未触碰）：`docs/plan/active/` 下 `D-数据准入清单.md` 删除、
  `D-文献内容结构化.md` 修改、`README.md` 修改、`D00-数据准入清单.md` 新增（D 线改动）。
- 文件所有权：本文件 + G 线只读范围（`backend/app/services/{graph_service,recommend_service}.py`、
  `backend/app/agent/tools/`、`output/graphs/`、数据库运行态）。未修改任何代码。

## 1. 图谱资产 — 可用 ✅

| 资产 | 大小/规模 | 版本/构建时间 | 节点/边来源 | 口径 |
|---|---|---|---|---|
| `output/graphs/author_graph.json` | 52,932 B；300 节点、52 边 | `graph_metrics.json` `generated_at=2026-06-23T17:08:09Z` | `data/analysis/author_metrics.csv`（top 300 中心性）+ `author_collaboration_edges.csv`（共著边，权重≥2） | 概览图只导出 top 中心性节点；边仅保留节点集合内、权重不足阈值剔除 |
| `output/graphs/keyword_graph.json` | 174,046 B；300 节点、1607 边、65 社区 | 同上（同一批导出） | `keyword_metrics.csv` / `keyword_cooccurrence_edges.csv`（top 300 关键词） | 同上 |
| `output/graphs/paper_topic_graph.json` | 36,109 B；259 节点、320 边 | 同上 | LDA 主题模型（`TOPIC_COUNT=40`）+ `topic_keywords.csv` | topic↔keyword 二部关系 |
| `output/graphs/graph_metrics.json` | 427 B | 同上 | 由 `src/models/graph_export.py` 生成 | 提供节点/边计数与模型标注 |

- 构建脚本：`src/models/graph_export.py`（`make graph-export`）；维护级口径声明在该文件头部：
  概览图是首屏加速缓存，全量关系由数据库 service 层提供，不承诺完整性。
- 服务层：`graph_service.is_available() = True`；author+center 的 ego 图从 PostgreSQL
  `coauthor_edges` 表实时构建（依赖 `SCISCOPE_DB_DSN` + `psycopg`），keyword/topic center 为内存过滤。
- 验证（只读）：`graph_service.graph('keyword')` 返回 300 节点；`graph('author', center='Rui Zhang')`
  经 DB 实时构建返回 6 节点 5 边，center 解析为 UID `27372e6b…`。

## 2. 推荐资产 — 不可用 ❌（阻塞：缺 `paper_embeddings`，归 G03）

| 项 | 状态 |
|---|---|
| 依赖表 `paper_embeddings`（pgvector，`src/models/recommend.py` / `make recommend-model` 构建） | **不存在**（`to_regclass('paper_embeddings')` = NULL） |
| `recommend_service.is_available()` | `False` |
| `recommend(paper_id)` 行为 | 抛 `UndefinedTable` 异常（无结构化降级；降级契约留待 G02） |
| 现有替代资产 `chunk_embeddings` | 7,680 条；覆盖 3,472 篇论文；model=`intfloat/multilingual-e5-base`；`created_at=2026-08-04`（RAG 用，非推荐用） |
| 推荐所需其他表 | `papers`、`keywords`、`paper_keywords`、`paper_authors` 均存在（见 §4） |

- 评分参数（已冻结，供 G04 评测参照）：`CANDIDATE_POOL=50`、`W_SEMANTIC=0.6`、`W_KEYWORD=0.2`、
  `W_AUTHOR=0.1`、`W_RECENCY=0.1`、`MMR_LAMBDA=0.7`。

## 3. 趋势资产 — 可用 ✅（数据日期 2026-06-24）

| 资产 | 规模 | 构建时间 | 窗口与口径 |
|---|---|---|---|
| `models/trends/hot_keywords.csv` | 154,376 B | 2026-06-24 | `normalized_df_<year>` 年份列 **2022–2026**（2026 为 YTD）；MK 检验 + Sen's slope + momentum/burst + 生命周期 + 次年预测 |
| `models/trends/topic_trends.csv` | 14,361 B | 2026-06-24 | 主题级趋势（同窗口） |
| `models/trends/trend_scores.json` | 37,356 B | 2026-06-24 | 综合热度评分 |
| `data/analysis/keyword_trends.csv` | 全量关键词趋势（非 top） | 2026-06-24 | 无 MK，仅 growth_rate/momentum/burst |

- 构建脚本：`src/models/trends.py`（`make trend-model`）。
- 验证（只读）：`get_trends.run({'keyword': 'retrieval augmented generation'})` 返回完整统计：
  doc_count=5588、方向 `increasing`、MK p=0.089（显著）、sen_slope=0.02509342、lifecycle=`growth`。

## 4. 数据库运行态

- 容器：`sciscope-db`（`pgvector/pgvector:pg16`，Up 23 小时），端口 `5433→5432`。
- 连接：`postgresql://tim@localhost:5433/sciscope`（本地 trust 认证，只读探测成功）。
- ⚠️ 运行注意事项：Makefile 默认 `POSTGRES_DSN=postgresql://tim@localhost:5432/sciscope`，
  但本机 `5432` 被 `educanvas-db` 占用（需密码）；sciscope 实际在 `5433`。
  **本机运行后端/TUI 需显式传 `SCISCOPE_DB_DSN=postgresql://tim@localhost:5433/sciscope`。**
- 关键表行数（只读）：`papers=159,164`；`authors=624,208`；`keywords=104,321`；
  `paper_authors=954,837`；`paper_keywords=709,726`；`coauthor_edges=3,485,411`（正权重）。
- 论文年份窗口：1921–2026。
- 本机 python 环境说明：系统 `python3` 缺 `pgvector`；项目实际环境为
  `/opt/homebrew/Caskroom/miniconda/base/envs/ai/bin/python`（pgvector+psycopg 可用），即 `PROJECT_PYTHON`。

## 5. Agent 工具运行态（只读调用）

| 工具 | 结果 |
|---|---|
| `query_knowledge_graph`（community / keyword+center） | ✅ 社区主题与 ego 图正常 |
| `get_trends` | ✅ 趋势统计正常 |
| `recommend_papers` | ❌ 依赖缺失（`paper_embeddings` 不存在 → `recommend()` 抛异常，非结构化降级） |

## 6. 结论与下游衔接

- 图谱：**可用**（导出 JSON + DB 实时 ego 图）→ G01 可基于现有资产补来源/版本/关系类型元数据。
- 趋势：**可用**（2026-06-24 构建，窗口 2022–2026 YTD）。
- 推荐：**BLOCKED**（缺 `paper_embeddings`，依赖 2080 Ti 全量构建，归 G03）；当前只能看到
  `chunk_embeddings` 小规模向量。G02 需把「无 embedding 时的结构化降级」写入契约。

## 7. 验证记录（实际执行）

- 只读健康检查：`graph_service.is_available()=True`、`recommend_service.is_available()=False`（ai env）。
- `rtk psql postgresql://tim@localhost:5433/sciscope` 只读探测：`paper_embeddings`=NULL、
  `chunk_embeddings=7680`、`coauthor_edges=3485411`、`papers=159164`，退出码 `0`。
- 未运行项：无（本任务为只读盘点，未启动服务、未写库、未改代码）。
- 风险/回退：端口 5432/5433 映射因本机环境（educanvas 占用）而异，运行脚本时应以容器映射为准；
  基线数据日期（图谱 2026-06-23、趋势 2026-06-24、chunk 向量 2026-08-04）需在后续任务中作为版本字段显式输出。
