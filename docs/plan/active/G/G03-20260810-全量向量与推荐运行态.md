# G03｜全量向量与推荐运行态

- 状态：`PASS（远端全量运行态）`
- 日期：2026-08-10
- 基线：`2d22457`
- 边界：证明全量向量、索引和推荐接口在 2080 Ti 环境可运行；不证明推荐质量、用户价值或 reranker 提升。

## 运行约束

远端只运行一条重计算线：`CUDA_VISIBLE_DEVICES=0`，embedding batch 为 32，
`OMP_NUM_THREADS/MKL_NUM_THREADS/OPENBLAS_NUM_THREADS` 均不超过 4，并使用 `nice`/`ionice`。
启动前先跑 16 chunks 冒烟；运行中持续检查温度、显存、内存、磁盘和 OOM。停止阈值为 GPU
温度不低于 82°C、可用显存低于 4GB、磁盘低于 100GB、出现 OOM 或发现并行重计算。

## 全量结果

| 项目 | 结果 |
|---|---:|
| papers | 159,164 |
| chunks | 367,861 |
| chunk_embeddings | 367,861 / 367,861 |
| paper_embeddings | 159,164 / 159,164 |
| embedding model | `intfloat/multilingual-e5-base` |
| ANN indexes | 2 个已确认 |
| embedding 全量耗时 | 16:21.97 |
| 真实推荐烟测 | `status=ok`，返回 5 条结果 |

全量导入耗时 6:42，16 条 embedding 冒烟、全量 embedding、chunk ANN 索引、paper recommendation
构建和真实 `recommend_papers` 烟测均退出 0。没有独立训练或评测 reranker，因此不得把本任务写成
“重排质量已提升”；重排质量只能由后续检索/推荐评测证明。

## 资源与日志

运行中最高已记录温度 66°C、向量阶段显存约 1.7GiB；结束时 GPU 41°C、可用显存约 10.8GiB、
可用内存约 28.8GiB、系统盘剩余约 560GiB。全程无 OOM、无并行重计算、无安全阈值触发。

证据日志位于远端工作区 `logs/`：

- `g03-load-20260810T085944Z.log`
- `g03-embed-smoke-20260810T090725Z.log`
- `g03-embed-full-20260810T090808Z.log`
- `g03-chunk-index-20260810T092537Z.log`
- `g03-recommend-model-20260810T092611Z.log`
- `g03-recommend-smoke-20260810T092655Z.log`

首次导入启动因未继承凭据文件在写入前失败，修正后完整重跑成功；最终一次组合对账命令存在 shell
引号错误，随后以分开的只读查询完成对账。两次失败都未被包装为成功，也未造成重复重计算。

## 下游边界

G03 关闭了 G04b 的全量 embedding 工程前置。G04b 可以开始 popularity/TF-IDF/dense/当前推荐的
自动基线、覆盖率、相关性、多样性、新颖性和错误分析；没有真实用户或领域专家记录时，仍不得宣称
推荐提升科研效率或具有已证明应用价值。
