# T｜TUI 科研工作流

- 状态：`active`（T00、T01、T02、T03 `PASS`；T04 `PENDING`）
- 负责人：Go TUI/演示负责人；复核：后端负责人 + 非技术评审者
- 当前领取：T02 已完成；下一步 T04（等待 A04 与真实端到端 trace）；依赖上游：[A 科研问答与协议](A-科研问答与协议.md)、
  [G 知识图谱与研究发现](G-知识图谱与研究发现.md)、[E 证据 L3 与国赛证明](E-证据L3与国赛证明.md)
- 交付边界：Go TUI 是 SSE 协议消费者和评委工作流界面，不能承担 PDF 解析、向量检索或
  LLM 推理；不新增大型 Web 前端。

## 已确认事实与诚实边界

| 事实 | 代码依据 | 当前不能据此声称的能力 |
|---|---|---|
| TUI 通过 SSE 消费后端事件 | `tui/stream.go`、`tui/README.md` | TUI 本地具有完整科研智能内核 |
| 已有 `/verify`、`/review`、`/trend`、`/recommend`、`/timeline` 等命令 | `tui/slash.go` | 每一个命令的后端数据依赖都已在当前机器可用 |
| TUI 可把自然语言任务交给 Agent | `tui/` 的会话/命令实现 | 已能上传并解析任意新论文或交互式渲染知识图谱 |

## 最简依赖图

```text
T00 能力与事件盘点 → T01 命令/黄金任务 → T02 证据与图谱卡 → T04 端到端演练
A02 答案与引用合同 ──────────────────────┘                         ├→ E06
G02 图谱/推荐查询合同 ───────────────────┘                         │
E03 争议三读路径 ─────────────────────────────────────────────────┘
T03 依赖故障降级 ──────────────────────────────────────────────────┘
```

## 原子任务

### T00｜TUI 能力、事件与运行依赖盘点

- 状态：`PASS`（产物已生成，验收待项目负责人复核）；依赖：无。
- 产物：[T00 TUI能力事件依赖盘点](T/T00-TUI能力事件依赖盘点.md)（基线 commit `67e275a`，2026-08-05）。
- 只读范围：`tui/`、SSE/OpenAPI 文档、现有 golden session；不得为“补功能”先改文案。
- 做什么：逐项列明现有命令、请求路径、消费的 SSE 事件、所需后端工具/模型/数据库，以及当前
  验证等级；将不能做的“新 PDF 解析”“交互图谱浏览”明确排除。
- 验收：TUI 能力表与 A/G/E 的输出合同一一对应，无幽灵命令。
- 验证：`rtk go test ./...`（在 `tui/`）和静态调用链审查；在线状态另列。

### T01｜评委可复现的命令与黄金任务

- 状态：`PASS`（2026-08-10；证据见 [T/T01-20260810-评委黄金任务与帮助口径](T/T01-20260810-评委黄金任务与帮助口径.md)）；依赖：T00、A02。
- 文件所有权：`tui/` 命令/帮助文本、golden session、TUI 测试；不得改变 Agent 推理策略。
- 做什么：把 `/review`、`/verify`、`/recommend`、`/trend` 绑定到可解释的任务语义和输入模板；
  让帮助文本说明证据边界、所需服务和数据未就绪时的行为。
- 验收：至少一个“提出 claim → 查看正反/不足证据”的黄金任务和一个“相关论文/方向线索”任务；
  任务不依赖人工临场输入。
- 验证：Go 单元测试 + 固定 SSE fixture；真正后端演练留 T04。
- 完成摘要：`/help` 与 CLI `--help` 已内置两条固定黄金任务（claim 核查、研究线索），并把
  `/trend` 的“描述性趋势”边界、`/recommend` 的 `paper_embeddings` 依赖和 `/verify` 的
  “支持/反驳/证据不足”约束直接写进帮助口径；同时把 `/review`、`/trend`、`/recommend`
  的命令描述改成不夸大能力的版本，新增帮助块测试与命令帮助测试。

### T02｜证据、图谱与推荐结果卡

- 状态：`PASS`（2026-08-10；证据见 [T/T02-20260810-证据图谱推荐结果卡.md](T/T02-20260810-证据图谱推荐结果卡.md)）；依赖：A02、G02、E03。
- 文件所有权：`tui/` 渲染和事件映射、相关 Go 测试；不在 TUI 重新计算关系或 stance。
- 做什么：以最小卡片呈现论文/证据句/立场/置信或拒答、图谱关系回链、推荐理由与数据版本；
  争议显示为同一 canonical claim 的正反证据，不能把相反句子硬拼为争议。
- 验收：卡片中的每个事实都能进入 paper/来源链；空结果和低置信不被视觉弱化成肯定答案。
- 验证：快照测试或事件 fixture；由 A/G/E 提供一条真实端到端样本后人工核对。
- 完成摘要：TUI 新增 A02 `answer-contract/v1` sidecar 卡、`verify_claim` 的 stance/置信度/证据句/来源链卡，
  以及 G02 `recommend_papers`、`query_knowledge_graph`、`get_trends` 和 E03 `list_disputes` 的结构化结果卡；
  `unavailable/empty/evidence_insufficient/dependency_failure` 全部按 fail-closed 渲染，不把空结果或依赖缺失伪装成成功。

### T03｜不可用依赖的诚实降级

- 状态：`PASS`（2026-08-05 独立复核通过）；依赖：T00。
- 产物：[T03-TUI依赖诚实降级.md](T/T03-20260805-TUI依赖诚实降级.md)（基线 commit `67e275a`，2026-08-05）。
- 文件所有权：`tui/` 的错误状态、重试和帮助文本及测试；不绕过服务端安全控制。
- 做什么：区分 API 不通、数据库/向量不可用、LLM 超时、无证据、权限限制；提示下一步而非
  伪造结果或无限重试。
- 验收：每种失败都有清楚、非技术评委能理解的状态；会话可保存/重试且不丢失失败原因。
- 验证：模拟 SSE 错误/超时的 Go 测试，人工查看文案。

### T04｜本机端到端演练与离线回退

- 状态：`PENDING`；依赖：T01、T02、T03、A04；争议场景还依赖 E03。
- 文件所有权：演示脚本、硬件清单、录屏/离线 fixture、TUI 必要修复；不生成或篡改线上指标。
- 做什么：按固定机器清单演练本机全量工作流，记录启动、首事件、检索、核查、推荐/图谱卡和
  故障切换；准备不联网时可展示真实预录事件的只读回退。
- 验收：连续三次计时演练；在线、离线回退和失败提示均有证据，不能把录屏说成实时结果。
- 验证：`rtk make tui-demo`（若实现入口存在）、`rtk make test-backend`、TUI 测试及演练记录。

## 证据台账

| 任务 | 状态 | PASS 所需证据 |
|---|---|---|
| T00 | `PASS` | [命令—SSE—依赖注册表](T/T00-TUI能力事件依赖盘点.md) |
| T01 | `PASS` | [T/T01-20260810-评委黄金任务与帮助口径](T/T01-20260810-评委黄金任务与帮助口径.md)：黄金任务、帮助块与 Go 测试 |
| T02 | `PASS` | [T/T02-20260810-证据图谱推荐结果卡.md](T/T02-20260810-证据图谱推荐结果卡.md)：结构化结果卡、A02 合同 sidecar 与 Go 测试 |
| T03 | `PASS` | [故障模拟与人工可读性检查](T/T03-20260805-TUI依赖诚实降级.md) |
| T04 | `PENDING` | 三次演练、硬件清单、离线回退证据 |
