# 当前执行计划

active 目录只保留仍需要开发或验收的任务线。每条线只拥有自己的代码/文档边界；完整原子
任务、验收和局部依赖图写在对应文件中。

根目录只放五条主计划；各原子任务的开发报告、命令输出摘录、人工核对和证据索引必须放入
对应的 `D/`、`G/`、`A/`、`T/`、`E/` 文件夹，不能散落在根目录或聊天记录中。

| 分配文件 | 状态 | 下一领取任务 | 不可越过的边界 |
|---|---|---|---|
| [D 文献内容结构化](D-文献内容结构化.md) | `active` | D04 真实 20 篇盲审；D05 等 D04 收口 | 不在 TUI 上传任意 PDF；受控摄取、解析与抽取留在 Python 数据层 |
| [G 知识图谱与研究发现](G-知识图谱与研究发现.md) | `BLOCKED` | 等 2080 Ti → G03/G04b | 不重建 Web 图谱；先交付可查询的图谱摘要、趋势和推荐证据 |
| [A 科研问答与协议](A-科研问答与协议.md) | `PENDING` | 等待 D05 + E02 → A02 | 不复制第二个 Agent Loop；复用现有工具注册表与 SSE 合同 |
| [T TUI 科研工作流](T-TUI科研工作流.md) | `PENDING` | 等待 A02 → T01 | Go 只消费 SSE，不承载解析、检索或推理 |
| [E 证据 L3 与国赛证明](E-证据L3与国赛证明.md) | `active` | E02（Wave 2） | 不把开发夹具当金标准；不把实现基础当外部证明 |

## Wave 1｜已验收

`D01`、`E01`、`E05`、`A01`、`T03` 均已通过独立复核。E01 仍只是流程试运行，
不构成正式金标准。

## Wave 2｜已完成代码复核，外部证据仍按原门禁

`D03`、`D05`、`G01`、`G02` 的实现与自动化回归已通过本轮复核；但 `D04` 的真实许可
20 篇盲审仍是外部门禁，D 线不得提前 PASS。`E02` 仍等待正式双语金标准与 SciFact 锚点评测，
故 `E03` 尚未解锁；`A02` 必须等待 D05 的正式收口与 E02 的证明门禁；`T01/T02` 必须等待
`A02`（且 T02 还等 `G02 + E03`）。

## Wave 3｜已完成

`G04a` 已通过代码、回测和产品出口复核：现有趋势模型 0/2 切分未胜过 naive，故 `get_trends`
在回测未通过、缺失或无效时 fail-closed 为“描述性趋势”，不输出未来预测数值。

## Wave 4｜门禁清除，不新增伪并行开发

当前没有能诚实并行开工的产品代码任务：G04b 等 2080 Ti；D04 等真实许可明确的 20 篇人工盲审；
E03 等正式双语金标准与 SciFact 锚点评测；A02/T01-T02 随之等待。Wave 4 的工作是获取这些真实
证据，而不是绕过依赖增加功能。

## 原子任务报告目录

| 任务线 | 报告目录 | 命名规则 |
|---|---|---|
| D | [D/](D/) | `D01-YYYYMMDD-短名.md` |
| G | [G/](G/) | `G00-YYYYMMDD-短名.md` |
| A | [A/](A/) | `A01-YYYYMMDD-短名.md` |
| T | [T/](T/) | `T03-YYYYMMDD-短名.md` |
| E | [E/](E/) | `E01-YYYYMMDD-短名.md` |

报告不是结论性宣传稿。每份必须包含：任务编号、基线 commit、实际修改文件及职责、执行的
`rtk` 命令和退出码、验收对照、未运行项、失败/阻塞、回退方式，以及 `rtk git diff --check` 和
`rtk git status --short`。只有通过审核后才更新主计划任务状态。

## 最简依赖图

```text
并行启动（不改产品语义）
E00 证据基线/台账 ─→ E01 20 条试标 ─→ E02 L3 纪律 ─→ E03 同 claim 争议 ─→ E04 OpenCode 真调 ─┐
D00 数据基线 ─────→ D01 准入合同 ───→ D02 解析/切片 ──┬→ D03 信息抽取 → D04 质量评估 → D05 结构化出口 ─┤
G00 图谱/推荐基线 ──────────────────────────────────────┘→ G01 图谱资产 → G02 查询合同 → G04a 趋势回测 ────────────────┤
                                                                                  └→ G03 全量 embedding → G04b 推荐评测 ─────┤
A00 问答能力盘点 ────────────────────────────────────────┘→ A01 意图路由 → A02 引文/拒答合同 → A03/A04 问答验证 ─────┤
                                                                                                             ↓
                                     A02 + G02 + E03 ─────────────────────→ T01/T02 核心展示 → T04 本机演练 ─→ E07 演示证据 ─┤
                                     T00/T03（TUI 盘点/降级）可在任意时点先做 ────────────────────────────────────────────────┤
E05 报告口径 ───────────────────────────────────────────→ E06 PDF 重建 ────────────────────────────────────────────────────┤
                                                                                                                               ↓
                                                                                                                    E09 提交整合/独立复现

外部阻塞线（不阻塞上述技术主线）
讯飞数据 + 授权 + 领域联系人 ───────────────────────────────────────────────────────────────────────────────→ E08 真实场景证明 ─┘
2080 Ti 恢复 ────────────────────────────────────────────────────────────────────────────────────────────────→ G03 全量向量/重排 ─→ G04b
```

执行原则：G02 后可开始 G04a；D05 正式收口后可完成 A02；TUI 的核心卡片必须等待 A02、G02、E03，
但 T00/T03 可以提前。E09 不因讯飞数据未到而停住，只有在申报材料主张讯飞真实场景时才依赖 E08。

## 协作与状态

- **PASS**：验收证据可复核，才可解锁下游；**REVISE**：有明确可修缺口；
  **BLOCKED**：外部依赖或环境不成立；**PENDING**：尚未开始；**UNLOCKED**：前置未通过。
- 每次只领取一个原子任务；先记录基线 SHA、预存改动与文件所有权。
- 回报必须给出：任务编号、修改文件职责、验收到测试映射、实际 `rtk` 命令和退出码、
  未运行项、风险/回退，以及 `rtk git diff --check`、`rtk git status --short`。
- 2080 Ti 不可用时，G03 保持 `BLOCKED`；无领域专家时，E01 只能是试运行；无讯飞数据/授权时，
  E08 保持 `BLOCKED`。

历史归档见 [`../completed/README.md`](../completed/README.md)。
