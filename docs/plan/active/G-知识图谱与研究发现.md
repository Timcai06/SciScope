# G｜知识图谱与研究发现

- 状态：`active`（G00/G01/G02/G04a 已完成；Wave 0 技术门禁关闭，G03 可显式启动）
- 负责人：检索/图谱负责人；复核：项目负责人 + 领域专家
- 当前领取：Wave 0 完成 G03 环境段 → Wave 1 全量构建 → Wave 2 G04b；依赖上游：[D 文献内容结构化](D-文献内容结构化.md) 的稳定元数据
- 交付边界：交付可审计的作者/关键词/论文主题关系查询、相关文献推荐与**谨慎的研究方向辅助**；
  不声称自动发现科学新知，不重建大型 Web 图谱浏览器。

## 已确认事实与诚实边界

| 事实 | 代码依据 | 当前缺口 |
|---|---|---|
| 图谱服务读取预构建 author/keyword/paper-topic JSON，并可输出中心性/社区摘要 | `backend/app/services/graph_service.py` | TUI 当前没有交互式图谱可视化或 `/graph` 命令 |
| 相关论文服务可使用 embedding、关键词、作者与 MMR | `backend/app/services/recommend_service.py` | 全量 paper embedding 与线上可用性须重新验收 |
| Agent 已有趋势、推荐、图谱查询工具 | `backend/app/agent/tools/` | 文本工具输出不是用户价值或推荐质量的证明 |

## 最简依赖图

```text
G00 图谱/推荐基线 → G01 可追溯图谱资产 → G02 查询与解释合同 → G04a 趋势回测与描述性边界
                                                     └──────────────→ T02
D03 结构化字段 ────────────────────────────────────┘
Wave 0 远程环境 ─────────────────────────────────────→ G03 全量向量/重排能力 → G04b 推荐质量与用户评测
```

## 原子任务

### G00｜冻结图谱、推荐与趋势基线

- 状态：`PASS`（2026-08-05 复核点：详见 [G00 图谱推荐趋势基线](G/G00-图谱推荐趋势基线.md)）；依赖：无。
- 只读范围：`backend/app/services/{graph_service,recommend_service}.py`、`backend/app/agent/tools/`、
  `output/graphs/`、数据库运行态。
- 验收：分别记录图谱 JSON 版本、节点/边来源、推荐所需 embedding 表、趋势数据窗口和各自
  可用/不可用状态；环境未启动只能标 `BLOCKED`，不得猜测成功。
- 验证：保存一次健康检查、只读查询或明确的阻塞记录。

### G01｜可追溯知识图谱资产

- 状态：`PASS（代码/资产合同）`（2026-08-08 独立复核通过；验收证据见
  [G01-20260805-知识图谱资产](G/G01-20260805-知识图谱资产.md)）；依赖：G00、D02。
- 文件所有权：图谱构建脚本、`output/graphs/` 的生成说明、图谱测试；不改 TUI。
- 做什么：为节点和边增加来源论文、构建版本、关系类型、权重含义和缺失/去重规则；区分
  “共现关系”与“支持某科学结论”，后者只能由 E 线 L3 给出。
- 验收：任一展示关系能追溯至论文/字段；生成脚本可在小样例重复运行；共现不被显示为因果。
- 验证：小样例重建、关系回链抽样、schema 测试。

### G02｜图谱、推荐、趋势的查询解释合同

- 状态：`PASS（代码/查询合同）`（2026-08-08 独立复核通过；验收证据见
  [G02-20260805-查询解释合同](G/G02-20260805-查询解释合同.md)）；依赖：G01。
- 文件所有权：`backend/app/services/`、`backend/app/agent/tools/`、对应 API/Agent 测试与文档；
  事件消费改动交给 T 线。
- 做什么：统一输出实体、关系/相似理由、时间范围、数据版本、来源论文和不可用原因；把
  “相关文献推荐”与“研究方向辅助”分开，方向只给可回溯趋势/空白线索，不给无证据创新断言。
- 验收：三个查询各有成功、空结果、依赖缺失的结构化响应；无 embedding 时明确降级。
- 验证：契约测试与三条固定查询产物。

### G03｜全量向量、重排与推荐可用性

- 状态：`READY`（2026-08-10 已核验代码、Python/CUDA、语料、模型资产与 PostgreSQL/pgvector；
  Wave 0 技术门禁已关闭；运行表仍为空，未导入语料、未运行全量 embedding，须由项目负责人显式启动）；
  依赖：Wave 0 远程环境、G00。
- 文件所有权：embedding/retrieval/recommend pipeline、评测配置；笔记本不得执行全量构建。
- 做什么：在远程机断点续跑全量 embedding，必要时完成 reranker 构建；记录模型、硬件、索引
  版本、耗时、失败恢复和成本。
- 验收：全量 `paper_embeddings` 与必要 chunk 向量具有清晰覆盖率；推荐接口在运行环境有真实
  成功记录，而不是 mock。
- 验证：仅在远程机执行 `rtk make embeddings` 及约定的运行时烟测；把实际命令、版本和退出码
  写回。只有远程环境、数据库和模型资产实测就绪后才能开始全量任务。

### G04a｜趋势描述、滚动回测与方向辅助边界

- 状态：`PASS（代码、回测与出口门禁）`（2026-08-08 Codex 复核通过；报告见
  [G04a-20260808-趋势回测与描述性边界](G/G04a-20260808-趋势回测与描述性边界.md)）；依赖：G02。
- 文件所有权：`evaluation/` 中趋势评测、趋势工具契约测试、报告图表与 `G/` 下任务报告；
  不改 embedding、推荐逻辑、金标准 stance 标注或 TUI。
- 做什么：对现有趋势数据做固定滚动时间切分，与 last-value/移动平均等 naive 基线比较；明确
  数据范围、样本数、失败案例和“描述性趋势”与“预测”的边界。
- 验收：可重建主表包含基线、分母、时间切分、MAE/sMAPE/方向指标和失败例；若不能稳定超过
  naive，工具与报告只可称“描述性趋势”，不得给出预测性方向卡。
- 验证：固定切分评测脚本、契约/回归测试和一份不含伪造分数的结果表。

### G04b｜推荐质量与用户评测

- 状态：`BLOCKED`；依赖：G03（全量 embedding 可用）以及真实用户/专家评测安排。
- 文件所有权：推荐评测样本、用户研究与报告图表；不改趋势评测结果。
- 做什么：为相关文献推荐建立相关性、多样性、新颖性、覆盖率和用户盲评，并和关键词/热门/
  dense 基线比较。
- 验收：真实 embedding 支撑的推荐成功记录、固定基线、用户或专家评价、失败案例；没有这些
  证据不得宣称推荐提升科研效率。

## 证据台账

| 任务 | 状态 | PASS 所需证据 |
|---|---|---|
| G00 | `PASS` | 图谱/模型/运行态基线（[G00 图谱推荐趋势基线](G/G00-图谱推荐趋势基线.md)：图谱可用、趋势可用、推荐 BLOCKED 缺 `paper_embeddings`） |
| G01 | `PASS（代码/资产合同）` | [G01 开发报告](G/G01-20260805-知识图谱资产.md)：18 项测试、全量与 DB 只读 SQL 复核一致、schema 契约；复核意见 HIGH 已修复 |
| G02 | `PASS（代码/查询合同）` | [G02 开发报告](G/G02-20260805-查询解释合同.md)：29 项新增测试、结构化三态、旧路径兼容、推荐不伪装可用；复核意见（旧路径伪装可用、趋势版本混源）已修复 |
| G03 | `READY` | 由项目负责人显式启动全量构建并保存真实运行记录 |
| G04a | `PASS（代码、回测与出口门禁）` | [G04a 报告](G/G04a-20260808-趋势回测与描述性边界.md)：滚动回测 0/2 切分胜出 naive → 描述性趋势；产品出口在回测未通过或缺失时 fail-closed；结果表 `output/eval/trends_backtest.{json,md}` |
| G04b | `BLOCKED` | G03 全量 embedding、推荐基线、人工/用户评测与错误分析 |
