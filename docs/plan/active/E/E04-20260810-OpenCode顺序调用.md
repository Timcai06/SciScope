# E04｜OpenCode 顺序调用证据

- 状态：`PASS`
- 日期：2026-08-10
- 边界：本任务证明 OpenCode 已对 SciScope MCP 完成真实顺序调用：先 `verify_claim(persist=true)` 写入，再读取
  `sciscope://disputes/recent`。它不证明自然语料 stance 质量；本次写入环境为受控 fixture MCP server。

## 结论

1. OpenCode 已完成真实工具调用：
   - `sciscope_verify_claim`
   - 输入：`{"claim":"E04 fixture coffee lowers cardiovascular risk","persist":true}`
   - 输出：`支持等级=存在争议`，`资产写入={"状态":"已写入","记录数":2}`
2. OpenCode 已在同一会话内完成第二步 resource 读取：
   - `read_mcp_resource(server="sciscope", uri="sciscope://disputes/recent")`
   - 返回包含同一条争议资产
3. 写入后的 live DB、Agent `list_disputes`、MCP `ClientSession.read_resource(...)` 和 OpenCode 自身 resource 读取都能读到同一条争议：
   - `claim = E04 fixture coffee lowers cardiovascular risk`
   - `paper_ids = ["E04-CONTRADICT", "E04-SUPPORT"]`
4. 期间发现并修复了 SciScope MCP resource 返回契约与真实客户端不兼容的问题：
   - 修复前：OpenCode / `ClientSession.read_resource(...)` 失败
   - 修复后：真实 OpenCode 重跑成功，E04 可按“真实 tool + resource 顺序调用”验收

## 本次受控环境

- 隔离数据库：临时容器 `sciscope-db-e04`
  - 数据卷：`sciscope_pgdata`
  - 端口：`127.0.0.1:55433 -> 5432`
  - 不触碰运行中的 `educanvas-db` / `educanvas-db-test`
- 临时 OpenCode 项目：
  - [`output/evidence/e04/opencode_fixture_project/opencode.json`](../../../../output/evidence/e04/opencode_fixture_project/opencode.json)
  - MCP command 显式 `cd` 回仓库，并运行 `backend.app.mcp_server_e04_fixture`
- 受控 fixture server：
  - [`backend/app/mcp_server_e04_fixture.py`](../../../../backend/app/mcp_server_e04_fixture.py)
  - 只替换 retrieval candidates / embedder / judge 输出；仍复用真实 `verify_claim`、真实 `record_stances` 和真实 `contradictions` view

## 关键证据

- OpenCode 原始事件流：
  - [`output/evidence/e04/opencode_run_e04.jsonl`](../../../../output/evidence/e04/opencode_run_e04.jsonl)
  - 其中可见：
    - `tool":"sciscope_verify_claim"` 且 `persist=true`
    - `资产写入={"状态":"已写入","记录数":2}`
    - 后续 `tool":"read_mcp_resource"` 成功返回同一条争议资产
- OpenCode 会话导出（canonical 证据）：
  - [`output/evidence/e04/opencode_session_e04_success.json`](../../../../output/evidence/e04/opencode_session_e04_success.json)
  - session id：`ses_014d383d3ffeAy6msHq5TBQUni`
  - 导出中同时保留：
    - `sciscope_verify_claim(persist=true)` 完整输入输出
    - `read_mcp_resource(server="sciscope", uri="sciscope://disputes/recent")`
    - 最终文字确认 `papers_paper_chunks_modified: false`
- live DB 写后对账：
  - `contradictions` 行：
    - `E04 fixture coffee lowers cardiovascular risk|1|1|2|{E04-CONTRADICT,E04-SUPPORT}`
- 直接读取成功（非 OpenCode 客户端）：
  - Agent tool `list_disputes` 返回同一 claim / paper IDs
  - `backend.app.mcp_server._read_resource("sciscope://disputes/recent")` 返回同一 claim / paper IDs
- 真实 MCP stdio 客户端读取成功：
  - `ClientSession.list_resources()` 返回 `sciscope://disputes/recent`
  - `ClientSession.read_resource(...)` 返回 JSON 文本内容
  - 这次修复同时验证了 SciScope MCP resource 契约与真实客户端兼容

## papers / paper_chunks 是否被修改

写前基线（55433）：

- `papers = 159164`
- `paper_chunks = 367861`
- `claim_evidence_stance = 0`
- `contradictions = 0`

写后（OpenCode 完成 `verify_claim(persist=true)` 后）：

- `papers = 159164`
- `paper_chunks = 367861`
- `claim_evidence_stance = 2`
- `contradictions = 1`

精确清理：

- `DELETE FROM claim_evidence_stance WHERE claim_norm = 'e04 fixture coffee lowers cardiovascular risk';`

清理后：

- `papers = 159164`
- `paper_chunks = 367861`
- `claim_evidence_stance = 0`
- `contradictions = 0`

结论：本次只写入并清理了派生 stance 资产；`papers` 与 `paper_chunks` 未变化。

## 运行与验证

### 代码测试

- `rtk proxy /opt/homebrew/Caskroom/miniconda/base/envs/ai/bin/python -m pytest backend/tests/test_e04_fixture_server.py backend/tests/test_e03_dispute_three_reads.py backend/tests/test_mcp_server.py backend/tests/test_disputes.py backend/tests/test_stance_store.py backend/tests/test_verify_claim_stance.py -q`
  - 结果：`35 passed, 1 skipped`
- `rtk proxy /opt/homebrew/Caskroom/miniconda/base/envs/ai/bin/python -m pytest backend/tests -q`
  - 结果：`524 passed, 1 skipped, 5 warnings`

### live 命令

- `rtk proxy docker run -d --name sciscope-db-e04 -p 127.0.0.1:55433:5432 -v sciscope_pgdata:/var/lib/postgresql/data pgvector/pgvector:pg16`
- `rtk proxy psql 'postgresql://tim@127.0.0.1:55433/sciscope' -f infra/postgres/stance.sql`
- `rtk proxy opencode mcp list`（在 `output/evidence/e04/opencode_fixture_project/` 下）
  - 结果：`sciscope connected`
- `rtk proxy /bin/zsh -lc 'cd .../output/evidence/e04/opencode_fixture_project && opencode run --auto --format json ... > .../opencode_run_e04.jsonl'`
  - 结果：真实 `verify_claim` 成功，随后 `read_mcp_resource` 成功
- `rtk proxy /opt/homebrew/Caskroom/miniconda/base/envs/ai/bin/python - <<\"PY\" ... ClientSession.read_resource(...) ... PY`
  - 结果：`READ_OK`，证明 SciScope resource 契约可被真实 MCP stdio 客户端读取

## 修复点

- 文件：[`backend/app/mcp_server.py`](../../../../backend/app/mcp_server.py)
- 真实问题：`_read_resource(...)` 返回值形状与当前 MCP low-level server 期望不兼容，导致真实客户端在读 resource 时失败
- 修复后：
  - 改为返回 `list[ReadResourceContents]`
  - `content` 为 JSON 字符串，`mime_type = "application/json"`
  - 直接带动 OpenCode 和 `ClientSession.read_resource(...)` 两条真实客户端路径恢复

## 诚实边界

- 本任务证明的是“真实 OpenCode client 顺序调用 + stance 资产写入/读取闭环”。
- 本任务不证明自然语料上的 stance 质量，也不把 fixture 输出包装成外部科学结论。
- 本任务只写入并随后清理 `claim_evidence_stance` 派生资产；不修改 `papers`、`paper_chunks` 原始文献语料。
