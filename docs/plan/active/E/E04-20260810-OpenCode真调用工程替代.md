# E04｜OpenCode 真调用工程替代（2026-08-10）

> 状态：`历史快照 / 已过时`
> 说明：本文件记录的是 E04 尚未打通真实 resource 读取前的中间态，不再作为 canonical 证据。
> 当前请以 [E04-20260810-OpenCode顺序调用.md](E04-20260810-OpenCode顺序调用.md) 为准。
> 旧结论“OpenCode 真调未完成 / 0 bytes 产物不可用”已被 2026-08-10 同日后续真实顺序调用证据取代。
> 基线 commit：`020744e`
> 结论：OpenCode CLI 在本机**已安装**，但当前受限执行环境下真实 `opencode mcp list` 未形成可保存的
> 真调产物；本轮只完成了一个**可复现的工程替代 harness**，证明“先 `verify_claim(persist=true)`，
> 再读 `sciscope://disputes/recent`”的 MCP 语义可被夹具化复现。

## 1. 修改文件与职责

| 文件 | 职责 |
|---|---|
| `backend/app/mcp_server_e04_fixture.py` | 为 E04 提供确定性 fixture：固定检索候选、embedder 和 stance judge，但仍复用真实 `verify_claim` 与真实 stance store 路径 |
| `backend/tests/test_e04_fixture_server.py` | 验证 fixture 下 `verify_claim(persist=true)` 会写入 SUPPORT+CONTRADICT 两条证据，并保持 paper ID 顺序与 claim 一致 |

## 2. 实测事实

### 2.1 OpenCode CLI 真实存在

```bash
rtk proxy zsh -lc 'command -v opencode >/dev/null 2>&1 && { echo FOUND; opencode --version; } || echo MISSING'
# FOUND
# 1.18.14
```

### 2.2 CLI 帮助与 MCP 子命令可见

```bash
rtk proxy zsh -lc 'XDG_DATA_HOME=/tmp/opencode-data XDG_CACHE_HOME=/tmp/opencode-cache XDG_STATE_HOME=/tmp/opencode-state opencode --help | sed -n "1,200p"'
rtk proxy zsh -lc 'XDG_DATA_HOME=/tmp/opencode-data XDG_CACHE_HOME=/tmp/opencode-cache XDG_STATE_HOME=/tmp/opencode-state opencode mcp --help | sed -n "1,220p"'
```

说明：

- `opencode` 主命令与 `mcp` 子命令都可执行；
- 默认用户目录下执行 `opencode mcp list` 会因日志文件路径不可写而失败；
- 将 `XDG_*` 重定向到 `/tmp` 后，`opencode mcp list` 在当前非交互受限执行里未返回可保存结果，
  因而**不能**把本轮写成“OpenCode 真调用已完成”。
- 工作区中存在 `output/evidence/e04/opencode_run_e04.jsonl`，但当前大小为 `0` bytes，
  只能视为未完成尝试的空壳，不能充当 E04 证据。

### 2.3 工程替代 harness 已完成

该 harness 不是伪造 stance 资产写入，而是：

- 复用真实 `verify_claim` 工具；
- 复用真实 `claim_evidence_stance` 写入路径；
- 只把检索候选、embedder、judge 替换成确定性 fixture；
- 用于证明 MCP/tool/resource 的**顺序与工程语义**，不是自然语料质量。

## 3. 验收与测试映射

| 验收点 | 证据 |
|---|---|
| fixture 下 `verify_claim(persist=true)` 会落同 claim 的正反证据 | `test_e04_fixture_installs_deterministic_writeable_verify` |
| fixture 检索候选 shape 稳定 | `test_e04_fixture_search_shape_is_stable` |
| E04 相关改动未打坏主干 | A02/E04 相关 53 passed；全量后端 520 passed、1 skipped |

## 4. 实际命令与结果

```bash
rtk test /opt/homebrew/Caskroom/miniconda/base/envs/ai/bin/python -m pytest \
  backend/tests/test_answer_contract.py \
  backend/tests/test_verify_claim_stance.py \
  backend/tests/test_agent_runtime.py \
  backend/tests/test_e04_fixture_server.py -q
# 53 passed in 0.18s

rtk test make test-backend
# 520 passed, 1 skipped, 5 warnings in 26.92s
```

## 5. 为什么现在仍是 REVISE

E04 的计划要求是：

1. OpenCode 先调用 `verify_claim(persist=true)`；
2. 再读取 `sciscope://disputes/recent`；
3. 留下可保存、可复核的真实调用产物。

本轮尚未满足第 3 点，因此不能 `PASS`。当前只可如实宣称：

- OpenCode CLI 存在；
- MCP 子命令入口存在；
- 受限执行环境下 `mcp list` 未形成可复核产物；
- 已准备好一个真实写路径的 fixture harness，可作为下一步真调前的工程兜底。

## 6. 下一步最小闭环

要把 E04 从 `REVISE` 推到 `PASS`，只需要补一条真实链：

1. 在可写日志目录与交互正常的终端执行 `opencode mcp list`；
2. 用 `opencode run ...` 或等价可保存方式，触发一次 `verify_claim(persist=true)`；
3. 紧接着读取 `sciscope://disputes/recent`；
4. 保存日志/录屏/JSON 产物，并与 E03 的 paper IDs 对账。

## 7. 回退

- 删除 `backend/app/mcp_server_e04_fixture.py`；
- 删除 `backend/tests/test_e04_fixture_server.py`；
- 不影响现有 MCP server 主实现。
