"""SciScope research-agent model layer.

This package holds the corpus-specific intelligence that turns the processed
literature corpus into an agent: embeddings/retrieval, trend forecasting,
recommendation, and knowledge-graph export. The LLM is only the generation
layer; reproducible models and indices live here.
"""
